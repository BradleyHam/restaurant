import React from 'react';
import classes from './CircleLine.module.scss'
const CircleLine = () => {
    return (
        <div className={classes.circle}>
        </div>

    );
}

export default CircleLine;